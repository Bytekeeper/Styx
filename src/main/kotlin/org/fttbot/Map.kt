package org.fttbot

import org.openbw.bwapi4j.WalkPosition
import javax.swing.text.Position

object Map {
    fun closestWalkablePosition(walkPosition: WalkPosition) : WalkPosition {
        throw NotImplementedError()
    }
}